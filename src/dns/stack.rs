// use super::{DnsResolver, DnsService};

pub struct Dns {
    // services: Vec<Box<dyn DnsService<Resolver = Box<dyn DnsResolver>>>>,
}

// struct BoxService {
//     inner: Box<dyn DynService<S, Request, Response = Response, Error = Error> + Send + Sync + 'static>,
// }
