<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleName</key>
  <string>RamaTransparentProxyExampleContainer</string>
  <key>CFBundleIdentifier</key>
  <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
  <key>CFBundleExecutable</key>
  <string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleShortVersionString</key>
  <string>$(MARKETING_VERSION)</string>
  <key>CFBundleVersion</key>
  <string>$(CURRENT_PROJECT_VERSION)</string>
  <key>CFBundlePackageType</key>
  <string>APPL</string>
  <key>LSMinimumSystemVersion</key>
  <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
  <!-- Must match Extension/Info.plist's NEMachServiceName (see comment
       there). Embeds $(CURRENT_PROJECT_VERSION) to dodge Apple
       FB21271237 — without the version suffix the launchd MachServices
       table doesn't refresh on sysext replacement and XPC lookups fail
       until the next reboot. -->
  <key>ProviderMachServiceName</key>
  <string>$(APP_GROUP_ID).provider.$(CURRENT_PROJECT_VERSION)</string>
</dict>
</plist>
