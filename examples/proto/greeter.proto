syntax = "proto3";

package rama.examples.greeter.v1;

// A tiny greeter service.
service Greeter {
    // Greet the caller by name.
    rpc SayHello(HelloRequest) returns (HelloReply);
}

message HelloRequest {
    string name = 1;
}

message HelloReply {
    string message = 1;
}
