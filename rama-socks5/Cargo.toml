[package]
name = "rama-socks5"
description = "SOCKS5 support for rama"
version = { workspace = true }
license = { workspace = true }
edition = { workspace = true }
repository = { workspace = true }
keywords = ["io", "async", "socks5", "network", "rama"]
categories = ["asynchronous", "network-programming", "web-programming"]
authors = { workspace = true }
rust-version = { workspace = true }

[package.metadata.cargo-public-api-crates]
allowed = []

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
targets = [
    "x86_64-unknown-linux-gnu",
    "x86_64-pc-windows-msvc",
    "x86_64-apple-darwin",
    "aarch64-apple-ios",
    "aarch64-linux-android",
]

[features]
default = []
# Define [`dial9`] runtime-telemetry events for SOCKS5 handshake stages
# (auth + connect-reply) and emit them at the corresponding lifecycle
# points in the client. Emission is a no-op when no
# `dial9-tokio-telemetry::TracedRuntime` is wired into the runtime; the
# events become live as soon as the consumer constructs a TracedRuntime.
#
# Building with this feature requires `--cfg tokio_unstable` (set
# workspace-wide in `.cargo/config.toml`).
#
# [dial9]: https://github.com/dial9-rs/dial9-tokio-telemetry
dial9 = ["dep:dial9-tokio-telemetry", "dep:dial9-trace-format"]

[dependencies]
byteorder = { workspace = true }
dial9-tokio-telemetry = { workspace = true, optional = true }
dial9-trace-format = { workspace = true, optional = true }
rama-core = { workspace = true }
rama-dns = { workspace = true }
rama-net = { workspace = true, features = ["http"] }
rama-tcp = { workspace = true, features = ["http"] }
rama-udp = { workspace = true }
rama-utils = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
parking_lot = { workspace = true }
tokio-test = { workspace = true }

[lints]
workspace = true
