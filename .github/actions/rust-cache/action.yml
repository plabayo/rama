name: Rust cache
description: Swatinem/rust-cache wrapped with retry on Windows (both restore and save legs are flaky there due to tar issues).

inputs:
  key:
    description: Cache key suffix
    required: true
  env-vars:
    description: Extra env vars to include in the cache key (space-separated names)
    required: false
    default: ""
  save-if:
    description: Conditional save (forwarded to rust-cache)
    required: false
    default: "true"
  cache-bin:
    description: Whether to cache ~/.cargo/bin (forwarded to rust-cache)
    required: false
    default: "true"
  workspaces:
    description: Workspace directories and their target dirs (forwarded to rust-cache), for projects outside the root workspace
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Rust cache (Windows, with retry)
      if: runner.os == 'Windows'
      uses: Wandalen/wretry.action@e68c23e6309f2871ca8ae4763e7629b9c258e1ea # v3.8.0
      with:
        action: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        attempt_limit: 3
        attempt_delay: 5000
        with: |
          key: ${{ inputs.key }}
          env-vars: ${{ inputs.env-vars }}
          save-if: ${{ inputs.save-if }}
          cache-bin: ${{ inputs.cache-bin }}
          # This inner mapping is itself a YAML string, so a multi-line value has to be a
          # quoted scalar or its later lines land unindented.
          workspaces: ${{ toJSON(inputs.workspaces) }}

    - name: Rust cache
      if: runner.os != 'Windows'
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      with:
        key: ${{ inputs.key }}
        env-vars: ${{ inputs.env-vars }}
        save-if: ${{ inputs.save-if }}
        cache-bin: ${{ inputs.cache-bin }}
        workspaces: ${{ inputs.workspaces }}
